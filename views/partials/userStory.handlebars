<div class="card story-card">
    <div class="card-body">
        <h4 class="card-title blog-name">{{name}}</h4>
        <h6 class="card-subtitle mb-2 blog-name">{{category}} {{dateOfExperience}}</h6>
        <h5>{{vaccineType}} </h5>
        <p class="card-text">{{body}}</p>
    </div>
</div>